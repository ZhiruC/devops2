- name : Playbook_test
  hosts: node1
  remote_user: vagrant
  tasks:
  - name: vider repo
    shell: "rm /home/vagrant/AppFlask -rf"
    becom: 'yes'
  - name: clone git
    git:
      repo: https://github.com/ZhiruC/MyAppFlask.git
      dest: /home/vagrant/AppFlask
      version: main
      clone: yes
      update: no
  - name: docker compose
    shell: "cd AppFlask; docker compose down; dockr compose up -d"
